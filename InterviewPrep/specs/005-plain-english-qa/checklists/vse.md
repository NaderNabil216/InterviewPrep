# VSE Register, Gate & Release Checklist: Very Simple English for Questions and Short Answers

**Purpose**: Requirements-quality validation (unit tests for requirements writing) covering the VSE
register requirements, the batch-gate and evidence requirements, and the release-train requirements
of feature 005-plain-english-qa — evaluated against spec.md, plan.md, research.md, data-model.md and
the two contracts.
**Created**: 2026-08-18
**Reviewed**: 2026-08-18 — all 46 items resolved; findings requiring artifact edits are noted below
and the edits are applied (spec.md, research.md, data-model.md, plan.md, tasks.md, both contracts).
**Feature**: [spec.md](./spec.md) · [plan.md](./plan.md) · [vse-register.md](./contracts/vse-register.md) · [batch-gate.md](./contracts/batch-gate.md)

**Note**: This checklist is generated by the `/speckit-checklist` command. It validates the
*quality of the requirements as written* — completeness, clarity, consistency, measurability,
coverage — not the implementation. Each `[x]` means the reviewer determined a requirements-quality
criterion is satisfied; it does NOT mean implementation work is complete. No item in this file is
pre-marked; all checkbox state belongs to the reviewer.

## Register Requirements Quality

- [x] CHK001 Is the register tiering by item kind (full VSE on the 545 `qa` items; plain-words
  half on the 84 `dsa`/`design`/`concept` items) explicitly bound to the item `type` field with no
  residue, as data-model.md §2 claims? [Clarity, Spec §Assumptions, Spec FR-016]
  → Satisfied: data-model.md §2 maps `type` → tier (545/84, "no residue"), spec Assumptions tier
  the register by item kind, FR-016 states the tiering.
- [x] CHK002 Is the exemplar-based register specified as *normative* (FR-002), with the
  review mechanism (compare a rewrite against the exemplars' target, not the floor rules)
  defined so a reviewer can apply it? [Measurability, Spec FR-002]
  → Satisfied: FR-002 normative; contract quotes both exemplars and the "not the target" versions.
- [x] CHK003 Is the authority hierarchy between the **floor rules and the reference batch**
  defined? Spec FR-026 and contracts resolve "exemplar vs reference batch" and "exemplar vs floor
  rule", but no document states which wins when a floor rule and the reference batch conflict.
  [Gap]
  → **Fixed**: spec FR-026 now states "between the floor rules and the reference batch, the
  reference batch wins"; mirrored in vse-register.md's "reference batch is the secondary authority".
- [x] CHK004 Does the spec itself state that the 25-word bound (FR-012) binds the 84 non-qa
  items, or is that binding only inferred in contracts/vse-register.md's tier table (FR-017 binds
  FR-011/013/014 but not FR-012)? [Ambiguity, Spec FR-012 vs contract §two tiers]
  → **Fixed**: spec FR-017 now binds FR-011/FR-012/FR-012a/FR-013/FR-014 onto the non-qa fields;
  research R-006 prose updated to match.
- [x] CHK005 Is the 25-word measurement defined by a single normative counter (the validator's,
  per R-006/contract V11), including how code-formatted spans and symbol-bearing tokens (`String?`,
  `{ }`) count toward it? [Clarity, R-006]
  → **Fixed**: R-006 and contract V11 now state the count is the validator's `words()` regex over
  the raw bullet text, with code spans/symbol-bearing tokens counting as the regex tokenizes them
  and no manual adjustment.
- [x] CHK006 Are the approximate sentence-length signals (~18 words per bullet sentence, ~12 per
  question sentence) specified as rework triggers rather than gates, with the per-sentence split
  basis defined? [Clarity, Spec floor rules, R-006]
  → Satisfied (split basis now also on the contract): spec floor rules call them signals; R-006
  and contract V12 define the per-sentence split on `.?!`.
- [x] CHK007 Is "reads simple" given an operational definition (e.g., US1's read-aloud first-pass
  parse, comparison to the exemplar targets) so the read-through's second question is applied
  consistently across 90 batches? [Measurability, Spec US1 Independent Test, FR-020]
  → Satisfied: US1/US2 Independent Tests (read aloud, first pass, no re-read) + FR-002's
  exemplar-target comparison, fixed in the batch gate (step 4).
- [x] CHK008 Is the instruction-verb ban (FR-003) specified as a floor for mechanical screening
  with the soft-stem class ("Walk me through…", "Can you explain…") defined and assigned to human
  review, with the named list closed but not a ceiling? [Completeness, Spec FR-003, US1 AS4]
  → Satisfied: FR-003 "floor, not a ceiling", soft-stem list named, assigned to the review step.
- [x] CHK009 Is the boundary between technical vocabulary kept verbatim (scope constraint 3, V4)
  and formal non-technical words to be replaced ("invoke" → "use") exemplified enough that the
  "unless the formal word is the technical term itself" exception is decidable? [Clarity, Spec
  constraint 3, contract V4]
  → **Fixed**: the "Everyday words" floor rule (spec + contract) now carries worked examples of
  the exception — `propagate` (exception propagation), `emit` (Flow), "implement an interface" —
  and the "both jobs" decision rule.
- [x] CHK010 Is the recorded-exception taxonomy (FR-008 question length, FR-012a word bound,
  mismatch repair) specified with the record format — item id + reason — so no exception is
  silent? [Completeness, Spec FR-008/FR-012a, R-010]
  → Satisfied: FR-008/FR-012a name item + reason; R-010 names all three kinds with record lines;
  FR-008a now formalises the question-length exception (see CHK041).

## Claim Preservation & Truth

- [x] CHK011 Is "claim" defined operationally (FR-011's list: version, API name, behavioural
  statement, limitation, caveat, causal link, recommendation) so a reviewer can enumerate claims
  per item and count them in the record? [Clarity, Spec FR-011, data-model.md §2]
  → Satisfied: FR-011 defines the list; data-model.md §2 repeats it as the formal definition.
- [x] CHK012 Does FR-014 specify the source-to-claim check in **both directions**, including
  claims a rewrite newly makes concrete (an assertion the original merely gestured at), and is that
  requirement applied to questions as well as bullets (FR-009 consistency)? [Completeness, Spec
  FR-014, FR-009, contract V6]
  → **Fixed**: spec FR-009 now extends the FR-014 both-directions check to version/date assertions
  retained in questions; contract V6 already read "the rewritten text" (both fields).
- [x] CHK013 Is it specified that claim preservation is enforced by the named human read-through
  (never by the validator's exit code), with per-item evidence recorded under FR-023? [Consistency,
  Spec FR-020/FR-023, batch-gate.md step 4]
  → Satisfied: FR-020 (validator never certifies a batch) + FR-023 (evidence-form records).
- [x] CHK014 Is the mismatch-repair rule (a bullet that stops matching its deep answer after 004's
  rewrite) specified with its boundary — repair the short answer only, record it, never touch the
  deep answer (004's field)? [Completeness, Spec Edge Cases, contract V13]
  → Satisfied: spec edge case + contract V13 + R-010(c).
- [x] CHK015 Is SC-002's "0 claims lost" backed by the defined evidence standard (per-item claim
  counts, source-to-claim mapping, lengths) rather than by assertion or a tick? [Measurability,
  Spec SC-002, FR-023, R-012]
  → Satisfied: FR-023's wording is quoted in SC-002; R-012 defines the record's evidence shape.

## Batch Gate & Evidence Requirements

- [x] CHK016 Are the batch-gate steps (validator, scope check, screens, read-through) specified
  with a fixed order and batch-level pass/fail semantics — no per-item exceptions, no batch
  splitting? [Completeness, Spec FR-019/FR-020, batch-gate.md]
  → Satisfied: batch-gate.md steps 1–4 in order; "On failure" = whole batch reworked and re-gated.
- [x] CHK017 Is FR-019a's "no new warning" specified against a run captured *immediately before
  the batch* with a diagnosis-and-attribution requirement, rather than a remembered baseline?
  [Clarity, Spec FR-019a]
  → Satisfied: FR-019a + batch-gate step 1 diagnosis table.
- [x] CHK018 Is the per-batch allowed-diff set specified mechanically (only `q`, `shortAnswer`,
  `updatedIn` may differ; `shortAnswer` exactly 3 bullets; id set byte-identical; no fenced blocks)
  so the reviewer's attention goes to truth and register, not bookkeeping? [Completeness, Spec
  FR-018, data-model.md §5]
  → Satisfied: data-model.md §5 table + T002 scope-check spec.
- [x] CHK019 Do the two read-through questions fail independently, with rework-in-place and
  full re-gating (all four steps, every item re-read) as the only recovery? [Completeness, Spec
  FR-020, batch-gate.md step 4 "On failure"]
  → Satisfied: FR-020 and batch-gate.md step 4 "fail independently… re-submitted to the whole gate".
- [x] CHK020 Is the batch record's content specified as evidence — claims compared, source-to-claim
  mapping, exceptions with reasons, question/bullet lengths vs baseline, verdicts — per FR-023 and
  R-012? [Measurability, Spec FR-023]
  → Satisfied: FR-023 list matches R-012's record contents.
- [x] CHK021 Is the fixed feature baseline (FR-021) specified with a recording mechanism (revision
  hash at feature start, R-001) and an explicit prohibition on comparing a batch against the
  previous commit? [Clarity, Spec FR-021, R-001]
  → Satisfied: R-001 (hash in the first task record); contract "The baseline is fixed once".
- [x] CHK022 Does FR-021's text still describe the Kotlin concurrency carve-out (a pre-004
  baseline) as live, when R-001 records both 004 deliveries merged and the carve-out moot — is the
  stale wording an inconsistency that needs a recorded resolution? [Conflict, Spec FR-021 vs R-001]
  → **Fixed**: spec FR-021 rewritten — single baseline for every track, carve-out recorded as
  moot; spec Assumptions and the "Overlap with 004" edge case updated to the merged state
  (`fef2e12`, manifest `2026.08.19`). R-001's decision is now the spec's text.

## Near-Duplicate Drift Requirements

- [x] CHK023 Is FR-022's adjudication requirement specified as library-wide (gate 8 screens all
  629 questions, so cross-track collisions are in scope), with a defined verdict vocabulary
  (`distinct`/`merged`/`accepted` + reason) and an in-batch, never-deferred timing? [Completeness,
  Spec FR-022, R-008]
  → Satisfied: FR-022 + R-008 (library-wide, verdicts, in-batch).
- [x] CHK024 Is the adjudication ledger (`.claude/workflows/duplicates.json`) specified as the
  single append-only record, with its format enforced by gate 8? [Completeness, Spec FR-022, R-008]
  → Satisfied: R-008 (append-only, format enforced by gate 8); tasks.md Cross-Story File Overlap
  makes the serialisation constraint explicit.
- [x] CHK025 Is the *risk mechanism* — the register's common openings ("What is…", "When do you…")
  converging question wording and manufacturing pairs out of previously distinct questions —
  acknowledged in requirements so the screen is not treated as a formality? [Coverage, Spec Edge
  Case "Two questions drifting together", R-008]
  → Satisfied: spec edge case + R-008 rationale.

## Release & Calendar Requirements

- [x] CHK026 Are the release-dating constraints (gate-10 per-track windows ≤ 2026-09-06 /
  ≤ 2026-09-08, gate-11 ≤ 2026-09-13, from R-007/data-model.md §3) traceable to a spec requirement,
  or do they exist only in research and contracts? [Gap, Spec FR-024]
  → **Fixed**: spec FR-024 now requires each release date to fall inside the validator's
  ref-freshness windows (gate 10 per track, gate 11 stack snapshot), decided at the per-track
  calendar checkpoint.
- [x] CHK027 Is FR-024's "a track's release ships only when every item on that track has both
  fields rewritten" consistent with R-011's "a non-qa track whose every item is recorded
  already-simple ships no release at all" — is the zero-edit case explicitly reconciled? [Consistency,
  Spec FR-024 vs R-011]
  → **Fixed**: spec FR-024 now states the zero-edit case explicitly: no edit, no stamp, no version
  bump.
- [x] CHK028 Is the before-final-batch calendar checkpoint (project the release date, record a
  decision: proceed / finish now / re-verify refs) specified as a requirement with recorded
  outcomes, rather than an informal caution? [Completeness, R-007, batch-gate.md "checkpoint"]
  → **Fixed**: spec FR-024 now mandates the checkpoint (decided before the track's final batch,
  never discovered blocked at the release gate); tasks T017/T029/… carry it per track.
- [x] CHK029 Is the coalescing fallback (two completed tracks sharing one release and one
  `updatedIn`) reconciled with FR-024's per-track release wording and the summary's honesty about
  the `UPD` chip? [Consistency, R-007 vs Spec FR-024]
  → **Fixed**: spec FR-024 now states the coalescing rule (completed tracks only, one `releases[]`
  entry naming both, per-track releases remain the plan).
- [x] CHK030 Is the per-release gate-13 audit (up to 10 version-claim items, source-support
  check) traceable to a spec requirement, or does it exist only in contracts/quickstart? [Gap,
  batch-gate.md release gate]
  → **Fixed**: spec FR-014 now specifies the gate-13 audit (validator names up to 10 version-claim
  items; each claim survives and stays supported; audit recorded with the release).
- [x] CHK031 Is `updatedIn`'s move-once-at-release timing specified consistently across FR-024,
  data-model.md §3 (batches edit prose only) and the scope check's allowed-diff set? [Consistency,
  Spec FR-024, data-model.md §3, §5]
  → Satisfied: FR-024 (stamped at release), data-model.md §3 (moves once, at the release), scope
  check's allowed set is a ceiling, not a mandate.

## Acceptance Criteria & Success Criteria Quality

- [x] CHK032 Are all eight success criteria measurable with defined populations (545 qa, 84
  non-qa, 629 total, 1887 bullets) and no qualitative-only claims? [Measurability, Spec SC-001..SC-008]
  → Satisfied: each SC names its population and its count; re-verified against the committed tree
  (R-006 figures corrected — see CHK042).
- [x] CHK033 Is the measurement distinction between FR-008's no-growth rule (raw `q.length`) and
  FR-007's preview rule (`stripMarkdown(q).slice(0, 40)`) specified, including the spec's "roughly
  the first 40 characters" vs the contract's exact `slice(0, 40)`? [Ambiguity, Spec FR-007/FR-008,
  R-004, R-005, contract V10]
  → **Fixed**: spec FR-007 and US1 AS5 now state the exact truncation
  (`stripMarkdown(q).slice(0, 40)`, `assets/js/views/item.js:88-89`); the "roughly" wording is gone.
- [x] CHK034 Do the user stories' acceptance scenarios each trace to a functional requirement (and
  vice versa), so every "Then" clause is backed by a requirement and every FR has a testable
  scenario? [Traceability, Spec US1..US3 Acceptance Scenarios]
  → Satisfied: AS1..AS6 per story map to FR-001..FR-009 (US1), FR-010..FR-015 (US2), FR-016..FR-017a
  (US3); section headers corrected (see CHK040).
- [x] CHK035 Does the Independent Test of each user story specify an objective procedure (read
  aloud, count bullets, run validator, compare spans) that a reviewer can execute as stated?
  [Measurability, Spec US1..US3 Independent Tests]
  → Satisfied: read-aloud first-pass, bullet counts, `validate.mjs` exit 0, code-span comparison.
- [x] CHK036 Are all eleven named edge cases (two-part question, scenario-length, term-is-jargon,
  qualifier-over-bound, mismatch repair, version claim, drift, truncation, growth, 004 overlap,
  quiet claim loss) each mapped to at least one FR or success criterion? [Coverage, Spec Edge Cases]
  → Satisfied: each edge case cites its FR (FR-005, FR-008, constraint 3, FR-012a, FR-013/FR-020,
  FR-014, FR-022, FR-007, FR-008, FR-021, FR-020).

## Scenario & Edge Case Coverage

- [x] CHK037 Is there a verdict path for a **qa** item already meeting VSE (FR-017a's
  "already simple" verdict covers non-qa only), or is FR-001's unconditional "all 545 MUST be
  rewritten" the intended answer for qa? [Gap, Spec FR-001 vs FR-017a]
  → **Fixed**: spec FR-017a now records the decision — the verdict is a non-qa mechanism; a qa item
  is always rewritten (FR-001), never recorded as "already simple".
- [x] CHK038 Is there any guardrail against erosion of the exception path (e.g., a trend check or
  cap on recorded FR-008/FR-012a exceptions), or is "recorded exception" unbounded by design?
  [Gap, Spec FR-008/FR-012a, R-010]
  → **Fixed**: recorded as unbounded by design with a visibility guardrail — spec FR-012a and
  R-010 now state every exception is recorded and the batch record's length figures (FR-023/R-012)
  make erosion visible across batches; SC-001/SC-003 count recorded exceptions.
- [x] CHK039 Is the trivial case — a question shorter than 40 characters where truncation never
  occurs (539 of 545 qa exceed 40 today) — addressed so the preview screen's scope is defined?
  [Coverage, R-005]
  → **Fixed**: R-005 now covers the ≤40-char case (6 of 545, raw): the question is its own prefix,
  an exact-prefix collision means identical questions, and the verdict folds into gate 8
  adjudication.

## Consistency & Conflicts

- [x] CHK040 Do the spec's functional-requirement groupings match the user-story labels: FR-010..
  FR-015 are grouped under "US1" though they belong to US2, and the non-qa block is labelled "US2"
  though it is US3 — is the mislabelling harmless or a traceability hazard? [Conflict, Spec
  §Functional Requirements]
  → **Fixed**: both section headers corrected — short-answer block is (US2), non-qa block is (US3);
  labels now match the stories and tasks.md's [US1]/[US2]/[US3] markers.
- [x] CHK041 Is "FR-008a" — cited in data-model.md §1/§5 and batch-gate.md step 4 as the question-
  length exception — defined anywhere in spec.md, which only names FR-008 with its recorded-
  exception path? [Conflict, Spec FR-008 vs data-model.md/batch-gate.md]
  → **Fixed**: FR-008a is now defined in spec.md (question stays longer only as a recorded
  exception naming item and reason; the escape, not the budget); contract V9 and tasks.md coverage
  table reference it; the existing citations in data-model.md/batch-gate.md/quickstart.md now resolve.
- [x] CHK042 Do the population figures agree across spec, plan, research and data-model (545 qa /
  1635 bullets / 84 non-qa / 629 items / 1887 bullets / 89 packs / 90 batches / 13 tracks)?
  [Consistency, Spec Assumptions vs R-002]
  → **Fixed**: census figures all agree (re-verified: 545/60/19/5 types, 629 items, 89 packs, 1887
  bullets). The R-006 word-count table was re-measured against the committed tree (`fef2e12`,
  manifest `2026.08.19`) with the validator's counter — the first-pass figures did not reproduce
  and the table's stated totals disagreed with its own rows. Corrected: 423/1887 (22%) >25w, of
  which 413 (25.3%) on the qa tier; >18w 1372 (73%). Question lengths corrected: all 13–215
  (median 81), qa 34–215 (median 85), non-qa 13–68 (median 45). Updated in research.md, plan.md,
  tasks.md track headers and data-model.md §1.
- [x] CHK043 Are the release-summary strings ("Questions and short answers reworded in simple
  English (N items)." / "Task prompts and descriptions simplified (N items).") specified
  consistently across R-007 and batch-gate.md, with the "no release for an untouched track" rule
  spelled out? [Consistency, R-007 vs batch-gate.md release gate]
  → Satisfied: identical strings in R-007, batch-gate.md and every release task; the zero-edit
  rule now also in spec FR-024 (CHK027).

## Dependencies & Assumptions

- [x] CHK044 Is the dependency on 004's shipped output (merged per R-001, `fef2e12`) recorded as
  resolved, and are the spec's Assumptions about 004's deliveries landing still current — or do
  they need updating alongside FR-021? [Assumption, Spec Assumptions, R-001]
  → **Fixed**: spec Assumptions and the "Overlap with 004" edge case now state 004's deliveries
  are merged (`fef2e12`, manifest `2026.08.19`); the "pre-004 claim baseline" phrasing is gone
  (single feature baseline, FR-021).
- [x] CHK045 Are the ref-freshness assumptions documented with their measurement date (2026-08-18)
  and the re-verification fallback (re-reading primary sources, never re-stamping `checked` without
  re-reading, per Principle IV)? [Assumption, R-007, batch-gate.md release gate]
  → Satisfied: R-007 dates the measurement, names the windows and the three resolutions; batch-gate
  release-gate text forbids re-stamping without re-reading.
- [x] CHK046 Are non-applicable non-functional dimensions (performance, accessibility, app code)
  explicitly excluded in Out of Scope rather than silently absent? [Completeness, Spec Out of Scope]
  → **Fixed**: spec Out of Scope now excludes app behaviour in any non-functional dimension
  (performance, accessibility, security) — the feature changes no app code.

## Notes

- Items are numbered sequentially; this file was created new, starting at CHK001 (46 items, CHK001–CHK046).
- Each item is a requirements-quality question: answering "yes" means the requirements satisfy the
  criterion; a "no" with a note is a finding to resolve in the spec/contracts before
  `/speckit-tasks`.
- Review outcome 2026-08-18: all 46 items pass. 17 items were satisfied as written; 16 required
  artifact edits (spec.md: FR-007/FR-008a/FR-009/FR-012a/FR-014/FR-017/FR-017a/FR-021/FR-024/
  FR-026, floor rules, assumptions, edge case, Out of Scope, two section headers, US1 AS5;
  research.md: R-004/R-005/R-006/R-010; plan.md: risk figures; data-model.md §1; tasks.md: coverage
  table, track loads, check-refs filter; vse-register.md: V9/V11/V12, floor rules, hierarchy;
  batch-gate.md + quickstart.md: check-refs filter). The remaining items' "yes" answers are
  documented inline above.
- Relevant sources: [spec.md](./spec.md), [plan.md](./plan.md), [research.md](./research.md),
  [data-model.md](./data-model.md), [vse-register.md](./contracts/vse-register.md),
  [batch-gate.md](./contracts/batch-gate.md).